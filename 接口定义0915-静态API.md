# 接口定义0915-静态API

| 项 | 内容 |
|---|---|
| 接口名称 | 静态思政素材检索 |
| 接口地址 | `GET /v1/teaching/resources/static` |
| 用途 | 按教材定位检索可供勾选的思政素材（仅返回素材卡片所需字段） |

---

## 1. 请求约定

### 1.1 请求头

- `Authorization: Bearer <API Key>`

### 1.2 Query 参数

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| schoolLevel | String | 是 | 例如：`8年级上` |
| subject | String | 是 | 例如：`物理` |
| textbookVersion | String | 是 | 例如：`人教版` |
| chapter | String | 是 | 支持章编号或章标题匹配，例如：`第六章` |
| lesson | String | 是 | 支持节编号或节标题匹配，例如：`第4节` |

请求示例：

```http
GET /v1/teaching/resources/static?schoolLevel=8年级上&subject=物理&textbookVersion=人教版&chapter=第六章&lesson=第4节
```

---

## 2. 响应结构

```json
{
  "items": [
    {
      "ideologyID": "EV_PEP8U_PHYSICS_06_04_01",
      "sourcePage": 167,
      "ideologyKeyword": "黄河铁牛",
      "l1_label": "文化素养"
    }
  ]
}
```

字段说明：

| 字段 | 类型 | 说明 |
|---|---|---|
| ideologyID | String | 思政素材唯一标识，对应 `paragraph_id` |
| sourcePage | Integer | 素材来源页码，对应 `source_page` |
| ideologyKeyword | String | 素材关键词，对应 `keyword` |
| l1_label | String | 一级思政标签；由该素材 `ideology_l3_codes` 反查到所属 L1 标签 |

---

## 3. 业务规则

- 接口仅用于“素材勾选”，不返回全文段落、不返回二三级标签列表。
- `ideologyID` 供前端多选后回传给模型接口字段 `metadata.ideologyIDs`。
- 若某节无素材，返回 `items: []`，HTTP 状态码仍为 `200`。

---

## 4. 错误码（与资源接口一致）

| HTTP | code | 说明 |
|---|---|---|
| 400 | `INVALID_ARGUMENT` | 参数格式错误 |
| 401 | `UNAUTHORIZED` | 鉴权失败 |
| 404 | `RESOURCE_NOT_FOUND` | 教材/章节/小节不存在 |
| 503 | `RESOURCE_SERVICE_UNAVAILABLE` | 静态资源服务不可用 |
