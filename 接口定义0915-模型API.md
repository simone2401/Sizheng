# 教学 AI 统一对话接口文档（0915 模型API）

| 项 | 内容 |
|---|---|
| 接口地址 | `POST /v1/chat/teaching` |
| 路由方式 | 统一入口 + `metadata.chatType` 区分业务 |
| 支持场景 | `case_guide_study`、`lesson_plan_assist` |
| 服务形态 | 无状态，不保存 conversation history |
| 返回形态 | 同时支持流式（SSE）和非流式（JSON） |
| 协议字段 | `content`、`reasoning_content`、`usage`、`error`、`model` |

---

## 0. 一页速览

### 0.1 统一入口

- 所有教学 AI 对话统一调用：`POST /v1/chat/teaching`
- 业务区分字段：`metadata.chatType`

### 0.2 chatType 与业务映射

| chatType | 业务含义 |
|---|---|
| `case_guide_study` | 案例导引学习 |
| `lesson_plan_assist` | 课程思政备课助手 |

### 0.3 关键约束

- 模型服务无状态，不负责存储会话历史。
- `conversationId`、`turnId` 在请求/响应一级字段保留并透传。
- 请求 `stream=true` 返回 SSE；`stream=false` 返回 JSON。
- 未传 `stream` 时默认按 `stream=true` 返回 SSE。
- `lesson_plan_assist` 支持通过 `metadata.ideologyIDs` 定向指定思政素材。

---

## 1. 通用协议约定

### 1.1 请求头

- `Content-Type: application/json`
- `Accept: application/json`（非流式）或 `text/event-stream`（流式），仅作为客户端推荐请求头，服务端不强制校验；服务端以请求体 `stream` 字段为准决定返回格式。
- `Authorization: Bearer <API Key>`

API Key 由成研院后端持有并管理，浏览器前端不直接暴露长期有效 API Key。

### 1.2 请求体（统一结构）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| requestId | String | 否 | 请求唯一标识；为空时服务端自动生成（前缀 `REQ`）。 |
| conversationId | String | 否 | 会话标识；为空时服务端自动生成（前缀 `CNV`）。 |
| turnId | Integer | 是 | 当前轮次，最小值 `1`。 |
| messages | Array<ChatMessage> | 是 | 对话消息列表（至少 1 条，最后一条为本次用户消息）。 |
| metadata | Object | 是 | 业务元数据，且 `metadata.chatType` 为业务必填字段。 |
| stream | Boolean | 否 | 是否流式返回，默认 `true`。 |
| model | String | 否 | 请求期望模型标识；传入时用于本次请求，未传入时使用服务端 `ZHIPU_MODEL` 配置，默认 `glm-5.2`。 |

`ChatMessage`：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| role | String | 是 | 取值见下表（不开放 `SYSTEM`） |
| content | Array<ContentItem> | 是 | 内容数组，至少 1 条 |

`role` 取值：

| role | 说明 |
|---|---|
| `USER` | 用户输入 |
| `ASSISTANT` | 助手回复 |

`ContentItem`（当前版本）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| type | String | 是 | 固定 `TEXT` |
| text | String | 是 | 文本内容 |

---

## 2. 业务字段规范（metadata）

### 2.1 案例导引学习（`case_guide_study`）

> 案例内容由调用方提供，直接拼接在 `messages` 中传入；模型服务不负责案例资源管理。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| chatType | String | 是 | 固定 `case_guide_study` |
| schoolLevel | String | 是 | 学段 |
| subject | String | 是 | 学科 |
| textbookVersion | String | 是 | 教材版本 |
| chapter | String | 是 | 章 |
| lesson | String | 是 | 节 |

### 2.2 课程思政备课助手（`lesson_plan_assist`）

> 该 `chatType` 统一承载教案生成与教案修改/思政融合。  
> 修改场景的原教案内容由上游拼接进 `messages`，元数据结构保持一致。

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| chatType | String | 是 | 固定 `lesson_plan_assist` |
| schoolLevel | String | 是 | 学段 |
| subject | String | 是 | 学科 |
| textbookVersion | String | 是 | 教材版本 |
| chapter | String | 是 | 章 |
| lesson | String | 是 | 节 |
| knowledgePoints | Array<String> | 否 | 知识点列表（建议数组） |
| ideologyIDs | String | 否（建议传） | 素材 ID 列表，英文逗号分隔；每个值对应静态资源接口返回的 `ideologyID`（即 `paragraph_id`） |

`ideologyIDs` 说明：

- 传入后，备课助手在查询资源时仅使用这些素材 ID 对应的思政段落。
- 响应中的 `l1_labels` 与所选素材集合保持一致，不包含未选素材标签。
- 未传入时，保持原行为（按当前节匹配到的全部思政素材）。

---

## 3. 统一响应协议

### 3.1 响应顶层字段

| 字段 | 类型 | 必有 | 说明 |
|---|---|---|---|
| requestId | String | 是 | 请求标识，与请求对应 |
| conversationId | String | 是 | 会话标识 |
| turnId | Integer | 是 | 当前轮次 |
| model | String/Null | 是 | 实际使用模型；可为 `null` |
| l1_labels | Array<String> | 是 | 一级思政标签列表；可为空数组 |
| content | String | 是 | 输出内容；非流式为完整正文，流式为当前增量 |
| reasoning_content | String/Null | 是 | 模型思考过程；非流式为完整内容，流式为当前增量 |
| finishReason | String/Null | 是 | 结束原因（见 3.2） |
| usage | Object/Null | 是 | token 使用量；流式通常在结束片段返回 |
| error | Object/Null | 是 | 错误信息；成功时为 `null` |

字段语义：

- `lesson_plan_assist`：`content` 返回生成或修改后的 Markdown 教案；`reasoning_content` 返回教材分析、教案对齐、思政标签匹配等模型思考过程。
- `case_guide_study`：`content` 返回案例导引对话文本；`reasoning_content` 返回模型推理过程文本（可按产品策略选择展示）。
- `l1_labels`：来源于本次请求对应资源中思政素材的一级标签集合；在 `lesson_plan_assist + ideologyIDs` 场景下，仅来自所选素材。

### 3.2 finishReason 枚举

| 值 | 含义 |
|---|---|
| `stop` | 正常完成 |
| `length` | 因长度截断 |
| `tool_calls` | 工具调用触发结束 |
| `content_filter` | 命中内容过滤 |
| `error` | 失败结束 |
| `null` | 流式过程中尚未结束 |

### 3.3 usage 结构

| 字段 | 类型 | 说明 |
|---|---|---|
| inputTokens | Integer | 输入 token 数 |
| outputTokens | Integer | 输出 token 数 |
| totalTokens | Integer | 总 token 数 |

### 3.4 error 结构

| 字段 | 类型 | 说明 |
|---|---|---|
| code | String | 错误码 |
| message | String | 错误描述 |
| retriable | Boolean | 是否可重试 |

---

## 4. 返回方式

### 4.1 流式返回（`stream=true` 或缺省）

- HTTP：`200 OK`
- Content-Type：`text/event-stream`
- 帧格式：`data: <JSON>\n\n`

说明：

- 中间分片：`finishReason=null`，`usage=null`。
- 终止分片：`finishReason!=null`，通常携带 `usage`。
- 服务端可能发送 `: ping` 注释帧保活连接；调用方应忽略注释帧，不作为业务数据处理。
- 调用方应优先以终止分片中的 `finishReason` 判定完成；若连接关闭但未收到终止分片，也应将已接收内容作为有效结果处理，并记录“非标准终止”日志。

### 4.2 非流式返回（`stream=false`）

- HTTP：`200 OK`
- Content-Type：`application/json`

示例：

```json
{
  "requestId": "REQ20260915000001",
  "conversationId": "CNV20260915000001",
  "turnId": 1,
  "model": "glm-5.2",
  "l1_labels": ["家国责任"],
  "reasoning_content": "已完成教材分析、教案对齐和思政标签匹配。",
  "content": "# 课程思政教学设计\n...",
  "finishReason": "stop",
  "usage": {
    "inputTokens": 1580,
    "outputTokens": 920,
    "totalTokens": 2500
  },
  "error": null
}
```

---

## 5. 错误响应规范

- HTTP：`4xx/5xx`
- 响应体仍遵循统一响应协议（非“裸错误体”）。
- `finishReason` 返回 `error`。
- `content` 返回空字符串。
- `reasoning_content` 允许为空字符串或 `null`。
- 调用方应以 `error != null` 或 `finishReason=error` 判断错误，不应依赖 `reasoning_content` 的字面值。

示例：

```json
{
  "requestId": "REQxxx",
  "conversationId": "CNVxxx",
  "turnId": 1,
  "model": null,
  "l1_labels": [],
  "reasoning_content": null,
  "content": "",
  "finishReason": "error",
  "usage": null,
  "error": {
    "code": "INVALID_ARGUMENT",
    "message": "...",
    "retriable": false
  }
}
```

---

## 6. 典型请求示例

- 本节示例为最小必填请求示例；接口同时支持按需携带 `requestId` 与 `conversationId`，传入时原样透传，未传入时由服务端自动生成。

### 6.1 案例导引学习（`case_guide_study`）

```json
{
  "stream": true,
  "turnId": 1,
  "messages": [
    {
      "role": "USER",
      "content": [
        {
          "type": "TEXT",
          "text": "（调用方拼接的案例内容 + 当前提问）"
        }
      ]
    }
  ],
  "metadata": {
    "chatType": "case_guide_study",
    "schoolLevel": "8年级上",
    "subject": "物理",
    "textbookVersion": "人教版",
    "chapter": "第六章",
    "lesson": "第4节"
  }
}
```

### 6.2 课程思政备课助手（`lesson_plan_assist`）

```json
{
  "stream": true,
  "turnId": 1,
  "messages": [
    {
      "role": "USER",
      "content": [
        {
          "type": "TEXT",
          "text": "请生成教案"
        }
      ]
    }
  ],
  "metadata": {
    "chatType": "lesson_plan_assist",
    "schoolLevel": "8年级上",
    "subject": "物理",
    "textbookVersion": "人教版",
    "chapter": "第六章",
    "lesson": "第4节",
    "knowledgePoints": ["密度与材料选择"],
    "ideologyIDs": "EV_PEP8U_PHYSICS_06_04_01,EV_PEP8U_PHYSICS_06_04_02"
  }
}
```

---

## 7. 兼容性与实现约束

- `/health/ready` 就绪返回 HTTP 200，依赖未就绪返回 HTTP 503。
- `MODEL_BACKEND` 仅支持 `mock` 和 `zhipu`；未显式关闭鉴权时，API Key 校验默认启用。
- 为保持前向兼容，调用方应采用“已知字段解析 + 未知字段忽略”策略。
- 对 `finishReason` 新增枚举值应按“结束态”统一处理，不应硬编码仅 `stop`。
- `lesson_plan_assist` 在传入 `ideologyIDs` 时，只筛选思政素材与标签；教材原文（`textbookChunks`）和课程标准（`curriculumStandards`）仍保持正常传入模型上下文。
