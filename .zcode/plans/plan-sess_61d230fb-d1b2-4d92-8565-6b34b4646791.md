1. 让请求级 `model` 生效：在 `PreparedTeachingRequest` 中保留/显式携带请求模型，修改 `TeachingChatService.generate/stream` 与模型客户端 `generate/stream` 签名，按“请求传 model 使用请求值，否则使用配置 `ZHIPU_MODEL`”选择模型；Mock 也返回实际选用的模型标识，智谱请求体的 `model` 使用解析后的模型。
2. 将 `reasoning_content` 调整为 `str | None`，并统一处理上游非流式/流式的缺失值和 null 值；案例模式仍按接口约定返回空字符串，备课模式允许真实智谱返回 null。同步检查 SSE 和错误响应，确保 null 不触发安全检查或未捕获异常。
3. 更新 README：明确生产和联调环境必须开启 Bearer API Key 鉴权；`MODEL_API_AUTH_DISABLED=true` 和 `RESOURCE_API_AUTH_DISABLED=true` 仅允许本地离线开发使用。补充服务端以请求体 `stream` 字段决定 JSON/SSE，`Accept` 只是推荐请求头，不做强制校验。
4. 更新接口定义文档 `/Users/simone/Desktop/智谱/接口/接口设计/课程思政接口定义0902.md`：明确请求级 model 的选择规则；明确 reasoning_content 允许 null 及 case 场景空字符串；补充流式中途错误因 SSE 已建立而使用 HTTP 200 + `finishReason=error` 事件；补充 Accept 非强制校验和本地鉴权开关说明。
5. 新增/调整测试：请求模型传递到 fake 模型客户端、无 model 时回退 `glm-5.2`、reasoning_content 为 null 的非流式/流式解析、鉴权说明对应行为、默认 stream 与 Accept 不强制校验。运行 `pytest -q`、语法检查，并进行本地 mock API 验证。