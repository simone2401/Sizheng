将新版 `app.model_api` 的上游智谱消息构造从“整段业务提示词放在唯一 system 消息”调整为明确的双消息结构，以满足智谱对 `messages` 的校验，同时落实你要求的职责划分。

1. 在 `app/model_api/teaching_service.py` 中扩展 `PreparedTeachingRequest`，分别保存 `system_prompt` 和 `user_prompt`，不再只保存单一 `prompt`。
2. 保留 `prompts.py` 内既有的两种系统角色约束：
   - `LESSON_PLAN_CONTRACT` 仅作为备课助手的 system 内容；
   - `SOCRATIC_CASE_PROMPT` 仅作为苏格拉底式案例导引的 system 内容。
3. 在 `TeachingChatService.prepare()` 中构造 user 内容，包含教材资源、思政标签、完整对话记录、教师当前请求，以及必要的业务上下文（如原教案和工作模式）。为避免重复，当前“对话记录”会保留教师最近一条请求；教师当前请求则额外用清晰字段标出，符合你提出的 user 侧数据范围。
4. 将 `ZhipuModelClient.generate()` 和 `ZhipuModelClient.stream()` 的调用签名改为接收 `system_prompt`、`user_prompt`，并在两种调用中提交：
   ```python
   "messages": [
       {"role": "system", "content": system_prompt},
       {"role": "user", "content": user_prompt},
   ]
   ```
   这样所有非流式与流式请求都有合法 user 消息，且角色约束与不可信教材/对话数据分离。
5. 更新 mock 客户端以接受相同签名，保持本地测试和真实客户端可互换。
6. 扩展 `tests/model_api/test_model_clients.py`：断言非流式请求含 system 和 user 两条消息、模型名继续正确透传；增加流式请求结构断言，防止未来重新退化为只有 system 的请求。运行针对模型 API 的 pytest 验证。

本次改动不改变 Apifox 请求格式、资源 API、API Key 配置或模型名；完成后继续使用 `MODEL_BACKEND=zhipu`、官方 `https://open.bigmodel.cn/api/paas/v4` 和 `glm-5.3-flash`。