# 课程思政教学 AI 联调说明

> 更新时间：2026-09-10。以下地址为联调环境，如有变更会另行通知。

## 1. 基本信息


| 项            | 内容                                                            |
| ------------ | ------------------------------------------------------------- |
| 统一入口         | `POST http://117.50.191.98/sizheng2/api/v1/chat/teaching`     |
| 鉴权           | `Authorization: Bearer chengyanyuan`                          |
| Content-Type | `application/json`                                            |
| 健康检查         | `GET http://117.50.191.98/sizheng2/api/health/ready`（200 为就绪） |
| 模型           | glm-5.2（真实模型，非 mock）                                          |
| 会话状态         | 服务无状态，历史对话由调用方在 `messages` 中自行拼接传入                            |


两种业务通过 `metadata.chatType` 区分：

- `lesson_plan_assist` — 课程思政备课助手（生成 / 修改教案）
- `case_guide_study` — 案例导引学习

## 2. 快速自测（健康检查）

```bash
curl -s http://117.50.191.98/sizheng2/api/health/ready \
  -H 'Authorization: Bearer chengyanyuan'
# 返回 200 即服务就绪
```



## 3. 非流式请求（stream=false，返回 JSON）

```bash
curl -s -X POST 'http://117.50.191.98/sizheng2/api/v1/chat/teaching' \
  -H 'Authorization: Bearer chengyanyuan' \
  -H 'Content-Type: application/json' \
  -d '{
    "stream": false,
    "turnId": 1,
    "messages": [
      {
        "role": "USER",
        "content": [
          { "type": "TEXT", "text": "请生成教案" }
        ]
      }
    ],
    "metadata": {
      "chatType": "lesson_plan_assist",
      "schoolLevel": "8年级上",
      "subject": "物理",
      "textbookVersion": "人教版",
      "chapter": "第六章",
      "lesson": "第4节",
      "knowledgePoints": ["密度与材料选择"]
    }
  }'
```



## 4. 流式请求（默认 stream=true，返回 SSE）

```bash
curl -N -X POST 'http://117.50.191.98/sizheng2/api/v1/chat/teaching' \
  -H 'Authorization: Bearer chengyanyuan' \
  -H 'Content-Type: application/json' \
  -d '{
    "turnId": 1,
    "messages": [
      {
        "role": "USER",
        "content": [
          { "type": "TEXT", "text": "请生成教案" }
        ]
      }
    ],
    "metadata": {
      "chatType": "lesson_plan_assist",
      "schoolLevel": "8年级上",
      "subject": "物理",
      "textbookVersion": "人教版",
      "chapter": "第六章",
      "lesson": "第4节"
    }
  }'
```

每个分片格式为 `event: message` + `data: {...}`，按 SSE 标准解析即可。

## 5. 案例导引学习示例（case_guide_study）

案例内容由调用方拼接在 `messages` 中传入：

```bash
curl -s -X POST 'http://117.50.191.98/sizheng2/api/v1/chat/teaching' \
  -H 'Authorization: Bearer chengyanyuan' \
  -H 'Content-Type: application/json' \
  -d '{
    "stream": false,
    "turnId": 1,
    "messages": [
      {
        "role": "USER",
        "content": [
          { "type": "TEXT", "text": "【案例】人民英雄纪念碑碑心石重达百吨，如何用密度知识估算它的质量？\n\n请基于该案例设计课堂导引问题。" }
        ]
      }
    ],
    "metadata": {
      "chatType": "case_guide_study",
      "schoolLevel": "8年级上",
      "subject": "物理",
      "textbookVersion": "人教版",
      "chapter": "第六章",
      "lesson": "第4节"
    }
  }'
```



## 6. 响应字段说明

顶层字段（每个 SSE 分片与 JSON 响应结构一致）：


| 字段                                  | 说明                                                       |
| ----------------------------------- | -------------------------------------------------------- |
| requestId / conversationId / turnId | 请求 / 会话 / 轮次标识，服务端自动生成或透传                                |
| model                               | 实际使用的模型，当前为 `glm-5.2`                                    |
| l1_labels                           | 一级思政标签数组（如 家国责任、文化素养 等）                                  |
| content                             | 正文输出（教案为 Markdown）；流式为增量，需自行拼接                           |
| reasoning_content                   | 模型思考过程；流式为增量；可能为空字符串或 null                               |
| finishReason                        | `null`=未结束；`stop`=正常结束；`error`=出错（其余枚举按结束态处理）            |
| usage                               | token 用量，通常在结束分片返回（inputTokens/outputTokens/totalTokens） |
| error                               | 成功为 null；出错时含 code / message / retriable                 |


注意事项：

1. 未传 `stream` 时默认按流式（SSE）返回，非流式必须显式传 `"stream": false`。
2. SSE 中可能出现注释型 keepalive 帧（如 `: ping`），请忽略，不作为业务数据。
3. 以结束分片的 `finishReason` 判定完成；若连接关闭但未收到结束分片，已收内容仍视为有效结果，建议记录"非标准终止"日志。
4. 服务无状态：多轮对话时，请把历史 USER / ASSISTANT 消息一并放入 `messages`，最后一条为本次用户输入。
5. 解析请采用"已知字段解析 + 未知字段忽略"策略，保持前向兼容。
6. 完整接口协议见附件《课程思政接口定义0902-修订版.md》。



## 7. 页面联调（可选）

如需在浏览器中人工验证，可打开联调页面（与接口同环境）：

- 地址：`http://117.50.191.98/sizheng2/`
- 页面内 API 地址填：`http://117.50.191.98/sizheng2/api`
- API Key 填：`chengyanyuan`

支持流式/非流式切换、reasoning_content 与 content 分栏展示、手动中止请求。

---

*联调期间接口地址或 Key 如有变更，我们会提前同步。遇到 401 请检查 Key，遇到 5xx 可重试（error.retriable=true 时）。*